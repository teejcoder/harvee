# Architecture Overview

Stub. This page will describe the shipped shape of the system — routes, state machines, data layer, log surface — once Phase 3 lands (per `.memory/implementation-plan.md` Step 7.1).

For the current source of truth, see:

- `.memory/overview.md` — product intent
- `.memory/state-transitions.md` — state machines and transition-log schema
- `.memory/domain-model.md` — business rules
- `.memory/conventions.md` — IDs, casing, correlation IDs, SQL, tests
- `.memory/tech-stack.md` — pinned versions and repo layout
