# State Machines

Stub. This page will render the Mermaid diagram from `.memory/state-transitions.md` and summarize the three lifecycles (client/project/task, time entry, invoice) once the machines are implemented (Phase 2).

For the current source of truth, see `.memory/state-transitions.md`.
