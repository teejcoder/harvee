# Data Model

Stub. This page will summarize the SQLite schema — tables, columns, foreign keys — once the migrations land (Phase 1).

For the current source of truth, see `.memory/domain-model.md` §10.
