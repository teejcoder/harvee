# Running Locally

Stub. This page will document the local dev workflow once the app is runnable end-to-end.

For now:

```bash
pnpm install
pnpm dev        # SvelteKit on http://localhost:5173
pnpm docs:dev   # this docs site on http://localhost:5174
pnpm test       # Vitest
```

See `README.md` for the current quickstart.
